interface:
  display_name: "Source Grounded Coder"
  short_description: "Disciplined coding workflow"
  default_prompt: "Use $sgc to make a source-grounded code change, verify it, and report the evidence."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
