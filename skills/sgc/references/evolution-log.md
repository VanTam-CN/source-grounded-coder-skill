# Evolution Log

Captured failures and candidate rule updates. See `evolution-method.md` for the distillation pipeline.

Entries are stored in the **project-local** `.sgc/` directory, not in the installed skill package.

## Active Entries

(No active entries yet. Entries are appended here during normal work when the skill's guidance is insufficient.)

## Processed Entries

(Entries that became rules or were rejected appear here with their outcome.)
